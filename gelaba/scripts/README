GELABA XSLT scripts

libgmlcheck.xsl -- XSLT library that has a template name "check" that
checks the validity constraints on the GML language

libgmldtd.xsl -- XSLT library that outputs DTD elements and attributes

libgmlstrings.xsl -- XSLT library that contains templates to output
strings, localized by a global argument 'lang'. This library currently
supports 'en' and 'fr'.

gml2doc.xsl -- Outputs an HTML documentation from a GML file. The
arguments are 'check' (no|yes) for language checking, 'lang' for
documentation language, and 'css' to indicate the location of a CSS.

gml2dtd.xsl -- Performs checking and outputs a DTD from a GML file.

gml2elements.xsl -- Outputs the name of all elements defined in a GML
file. The parameter 'check' (yes|no) indicates whether to perform
checking or not.

gml2dict.xsl -- Outputs an XML file containing the dictionaries
contained in a GML file.

gmldoc.css -- A default CSS for files produced by gml2doc.xsl.